<html lang="en">
    <head>
        <% include ../partials/head %>
    </head>
    <body class="container">
        <header>
            <% include ../partials/nav %>
        </header>
        <main>
            <div class="card">
                <div class="card-header"><%= title %></div>
                <div class="card-body">
                    <h2><%= title %></h2>
                    <% journals.forEach(journal => { %>
                    <h3><%= journal.title %><small> 
                        <a href="/journals/edit/<%= journal.id %>"><i class="fa fa-pencil"></i></a>
                        <a href="/journals/delete/<%= journal.id %>"><i class="fa fa-trash"></i></a>
                    </small></h3>
                    <p><%= journal.content %></p>
                    <p><%= journal.happenedAt.toDateString() %></p>
                    <p><%= journal.User.username %></p>
                    <% }) %>
                </div>
            </div>
        </main>
    </body>
</html>